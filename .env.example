# APP_URL: The public URL where this app is hosted (e.g., https://secureshare.com)
# Used for CORS and generating shareable links.
APP_URL="http://localhost:3000"

# PORT: The port the server will listen on.
PORT=3000

# DB_PATH: Path to the SQLite database file.
DB_PATH="secrets.db"

# NODE_ENV: Set to 'production' for production environments.
NODE_ENV="development"

# GEMINI_API_KEY: Optional (if AI features are added).
GEMINI_API_KEY=""

# Admin Dashboard Configuration
ADMIN_PASSWORD=""
JWT_SECRET="your_secret_at_least_32_chars_long_here"
